// Este arquivo controla as variáveis de ambiente de desenvolvimento.
export const __DEV_MODE__ = {
  // Mude para 'false' para testar o fluxo de login normal.
  // Mude para 'true' para pular o login e ir direto para a tela principal.
  BYPASS_LOGIN: true, 
};